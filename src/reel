#!/usr/bin/env python3
# Launcher for Reel — sets argv[0] so the shell sees "reel" not "python3"
import sys
import os

sys.argv[0] = "reel"
sys.path.insert(0, "/usr/lib/reel")

from main import main
sys.exit(main())
